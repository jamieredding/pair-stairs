import {Typography} from "@mui/material";

export default function Home() {
    return (
        <main>
            <Typography variant="body1">
                Use the pages above to either be offered some options for everyone to pair today,
                or manually pick some pairs yourself.
            </Typography>
        </main>
    );
}
